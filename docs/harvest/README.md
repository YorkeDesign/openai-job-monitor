# Harvest

Daily Harvestry ingest record for this project: one `YYYY-MM-DD-harvest.md` per day with a verdict per capture, and `proposals/` for adoptable captures awaiting a decision. Written by the `harvest-ingest` skill.
