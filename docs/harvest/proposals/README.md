# Harvest proposals

One file per adoptable Harvestry capture (template: `~/.claude/templates/harvest-proposal.md`). `status:` in the front matter is the gate: `awaiting-approval` until Simon sets `approved` or `rejected`; `implemented` carries the commit SHA.
